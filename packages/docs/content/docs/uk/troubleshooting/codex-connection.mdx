---
title: Підключення до Codex
description: Виправте проблему, коли OpenChamber не підключається до свого сервера Codex.
---

# Підключення до Codex

Якщо OpenChamber завантажується, але ніяк не виходить за межі стану «Codex перезапускається», або чат не відповідає, сервер, із яким він спілкується, недоступний. Пройдіться по цьому.

## Він застряг на «Codex перезапускається»

- дайте йому хвилину одразу після запуску — цей стан нормальний, поки сервер стартує
- перевірте, чи сервер живий, командою `openchamber status`
- перезапустіть його командою `openchamber restart`
- перегляньте деталі запуску командою `openchamber logs`

## Ви підключаєтеся до власного сервера

Якщо ви налаштували OpenChamber на використання наявного сервера, ще раз перевірте налаштування з [Codex Runtime](/uk/codex-server/):

- якщо адреса недійсна, OpenChamber ігнорує її й натомість запускає власний сервер — шукайте попередження `[config]` у журналах

## Усе ще не працює

- переконайтеся, що версія Node.js — `20` або новіша
- перевстановіть найновіший CLI
- відкрийте `http://localhost:3000` напряму, перш ніж тестувати будь-який тунель чи віддалене посилання

## Пов'язане

- [Codex Runtime](/uk/codex-server/) — як OpenChamber знаходить сервер і керує ним
- [Усунення несправностей](/uk/troubleshooting/) — інші поширені проблеми
