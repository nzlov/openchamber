# Generated Codex App-Server TypeScript Protocol

Generated with:

```bash
codex --version
codex app-server generate-ts --experimental --out packages/web/server/lib/codex/generated
```

Codex version:

```text
codex-cli 0.142.3
```

These files are development-time protocol artifacts. The current web server runtime is plain JavaScript, so server modules must not import these TypeScript files unless a server TypeScript build path is added intentionally.

