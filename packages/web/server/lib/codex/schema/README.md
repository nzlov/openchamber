# Generated Codex App-Server JSON Schema

Generated with:

```bash
codex --version
codex app-server generate-json-schema --experimental --out packages/web/server/lib/codex/schema
```

Codex version:

```text
codex-cli 0.142.3
```

Use these schemas as the source of truth for runtime validation work. Keep validation narrow: validate the app-server payloads OpenChamber consumes instead of exposing raw app-server payloads directly to shared UI.

